// TODO: start parsing 'pub' at top level for things like interface/struct/fn/var

// What i need is an array 'InterfacesImplemented' in a AST function declaration that gets added to
// then i can know stuff

type Foo i32

type Animal interface {
	Hello() -> i32
}

type Dog struct {
	pub mut i32 Age
	pub const i32* X, Y, Z
	mut i32 Weight
	const i32 Name
}

fn (dog Dog*) Hello() -> i32 {
	return 0
}

fn (dog Dog*) Other() {
}

fn main() -> i32 {

	const Animal animal

	const Foo x = 0

	return 0
}