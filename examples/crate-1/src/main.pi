type Foo u8;

type Animal interface {
	pub fn say_hi(this, Foo x, u16 y);
	fn bar() -> i16;
};

type Dog struct {
	u8 foo;
};

apply Animal to Dog {
	pub fn say_hi(this, Foo x, u16 y) {

	}

	fn bar() -> i16 {}
}

fn main() {
	
}