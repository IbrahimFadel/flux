mod test;
mod bar;

use pkg::test::do_test;

fn main() {
	i32 x = 0;
	// pkg::test::do_test(x);
	// do_test(x);

	pkg::bar::do_bar(x, 10);
}

/*

you should be able to define an interface simply as a collection of interfaces

type Foo interface {
	// methods...
} + Bar + Bazz

*/