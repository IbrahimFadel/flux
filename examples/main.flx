type Vec struct<T> {
	len u64,
	capacity u64,
	buf *T,
}

apply<T> to Vec<T> {
	fn new() -> Vec<T> => {
		Vec {
			len: 0,
			capacity: 0,
			buf: @flux.intrinsics.malloc(32),
		}
	}

	fn delete() => {
		let x = 0;
		if x == @flux.intrinsics.nullptr {
		}
		@flux.intrinsics.free(0);
	}
}
