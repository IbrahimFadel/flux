mod foo;
mod bar;
mod bazz;
mod test;
mod hi;

struct Foo {
    x i32,
    y f32,
    z Bar,
}

struct Bar {
    bar_field u64
}

fn main() {
    let test = 0;
    let x Foo = Foo {
        x: x,
        y: 1.0,
        z: Bar {
            bar_field: 100,
        }
    };
}