mod test;
mod bar;

use pkg::test::do_test;

fn bazz(i32 x, i5 y) -> i3 {

}

fn main() {
	i16 x = 0 + -2;
	let y = -10;

	pkg::bar::do_bar(foo::bar, y, z);
	bar();
	let z = x + 10;

	let foo = 0;
	i32 foo1 = 1.0;
}

/*

TODO: you should be able to define an interface simply as a collection of interfaces

type Foo interface {
	// methods...
} + Bar + Bazz
*/ 