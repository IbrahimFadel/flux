trait ToI32 {
	fn to_i32(u8 x, i32 y) -> i32;
}

type Foo struct {
	x i32,
}

apply ToI32 to Foo {
	fn to_i32(u8 x, i32 x) -> i32 => {
		0
	}
}

// TODO: parse where clauses, start typechecking generics
type Bar struct<T>
{
	x T,
}

fn ) => {
}