import "./__cstd.pi";

pub fn @free(i8 *ptr) -> void {
    free(ptr);
}

pub fn @malloc(i32 size) -> i8* {
    return malloc(size);
}

pub fn @memcpy(i8 *dest, i8 *src, i32 size) -> i8* {
    return memcpy(dest, src, size);
}